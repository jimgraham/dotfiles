# load dev, but only if present and the shell is interactive
if [[ -f /opt/dev/dev.sh ]] && [[ $- == *i* ]]; then
  source /opt/dev/dev.sh
fi

export SRC="$HOME/src/github.com/Shopify"
export PSRC="$HOME/src/github.com/jimgraham"

if [ -e /Users/jimgraham/.nix-profile/etc/profile.d/nix.sh ]; then . /Users/jimgraham/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
